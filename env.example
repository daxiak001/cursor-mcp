# 云端环境变量配置示例

# 环境
NODE_ENV=production

# 服务端口（Railway会自动分配）
PORT=3000

# 允许的跨域来源（多个用逗号分隔）
ALLOWED_ORIGINS=https://cursor.sh,http://localhost,https://*.cursor.sh

# 数据库（Railway会自动提供PostgreSQL）
# DATABASE_URL=postgresql://user:password@host:port/database

# API限流配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 日志级别
LOG_LEVEL=info

# 是否启用WebSocket推送
ENABLE_WEBSOCKET=true

# API认证（可选）
# API_SECRET_KEY=your-secret-key-here

# 监控配置
ENABLE_MONITORING=true
MONITOR_LOG_PATH=/tmp/logs/monitor.log

